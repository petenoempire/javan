[install]
# Supply-chain guard disabled: minimumReleaseAge was blocking fresh installs in
# the Lovable build sandbox when a required package version was younger than the
# window, causing "Preview has not been built" failures. Installs now resolve
# deterministically against the committed bun.lock.
minimumReleaseAge = 0
